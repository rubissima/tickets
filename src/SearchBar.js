
import React from 'react';

const SearchBar = () => {
    return (
      <div className='py-8'>
        <input type="text" placeholder="Search for events" className="input input-bordered w-full max-w-xs" />
      </div>
    );
};

export default SearchBar;
